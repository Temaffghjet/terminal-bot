import React from "react";
import ReactDOM from "react-dom/client";
import StableTradeApp from "./StableTradeApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <StableTradeApp />
  </React.StrictMode>
);
